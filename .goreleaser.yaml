version: 2

project_name: abnormal-mcp

before:
  hooks:
    - go mod tidy

builds:
  - id: abnormal-mcp
    main: ./cmd/abnormal-mcp
    binary: abnormal-mcp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: ["tar.gz"]
    format_overrides:
      - goos: windows
        formats: ["zip"]

checksum:
  name_template: checksums.txt
  algorithm: sha256

sboms:
  - artifacts: archive

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

dockers:
  - id: linux-amd64
    dockerfile: Dockerfile.release
    image_templates:
      - "ghcr.io/gregdog/mcp-server-abnormal:{{ .Tag }}-amd64"
    goos: linux
    goarch: amd64
  - id: linux-arm64
    dockerfile: Dockerfile.release
    image_templates:
      - "ghcr.io/gregdog/mcp-server-abnormal:{{ .Tag }}-arm64"
    goos: linux
    goarch: arm64

docker_manifests:
  - name_template: "ghcr.io/gregdog/mcp-server-abnormal:{{ .Tag }}"
    image_templates:
      - "ghcr.io/gregdog/mcp-server-abnormal:{{ .Tag }}-amd64"
      - "ghcr.io/gregdog/mcp-server-abnormal:{{ .Tag }}-arm64"
  - name_template: "ghcr.io/gregdog/mcp-server-abnormal:latest"
    image_templates:
      - "ghcr.io/gregdog/mcp-server-abnormal:{{ .Tag }}-amd64"
      - "ghcr.io/gregdog/mcp-server-abnormal:{{ .Tag }}-arm64"
